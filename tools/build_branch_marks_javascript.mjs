#!/usr/bin/env node
/** Build the local CP6 JavaScript package and installable BranchMarks starter. */
import {createHash} from "node:crypto";
import {cpSync,existsSync,mkdirSync,readdirSync,readFileSync,rmSync,writeFileSync} from "node:fs";
import {basename,dirname,join,relative,resolve,sep} from "node:path";
import {spawnSync} from "node:child_process";
import {fileURLToPath,pathToFileURL} from "node:url";

const ROOT=resolve(dirname(fileURLToPath(import.meta.url)),".."),PACKAGE=join(ROOT,"packages/javascript"),BRANCH=join(PACKAGE,"examples/branch-marks");
const OUTPUT_ROOT=join(ROOT,".work/dist/cp6"),VERSION="0.6.0",NAME="@procedurals/javascript";
const NOTICES=[join(ROOT,"LICENSE"),join(ROOT,"THIRD_PARTY_NOTICES.md")];
const BOUND=["catalog/operations/regular-grid.json","catalog/operations/gradient-noise-2d-01.json","catalog/operations/cyclic-palette.json","catalog/operations/gradient-path.json","catalog/operations/ordered-circle-filter.json","catalog/operations/seeded-circle-placement.json","catalog/operations/seeded-quadrant-partition.json","catalog/operations/seeded-triangle-points.json","catalog/operations/triangle-coordinate-map.json","catalog/operations/seeded-endpoint-branches.json","fixtures/operations/regular-grid.json","fixtures/operations/gradient-noise-2d-01.json","fixtures/operations/cyclic-palette.json","fixtures/operations/gradient-path.json","fixtures/operations/ordered-circle-filter.json","fixtures/operations/seeded-circle-placement.json","fixtures/operations/seeded-quadrant-partition.json","fixtures/operations/seeded-triangle-points.json","fixtures/operations/triangle-coordinate-map.json","fixtures/operations/seeded-endpoint-branches.json","evidence/conformance/branch-marks-javascript-parity.json","evidence/conformance/branch-marks-javascript-root-review.json","evidence/conformance/branch-export-compatibility-review.json","evidence/conformance/branch-tree-javascript-root-review.json","evidence/conformance/branch-p5js-native-root-review.json","design/port-batch-04.md"];
const OPERATIONS=[
  ["layout.regular-grid","regular-grid.js","regularGrid"],
  ["fields.gradient-noise-2d-01","gradient-noise-2d-01.js","gradientNoise2D01"],
  ["color.cyclic-palette","cyclic-palette.js","cyclicPalette"],
  ["paths.gradient-path","gradient-path.js","gradientPath2D"],
  ["sampling.ordered-circle-filter","circle-placements.js","orderedCircleFilter2D"],
  ["sampling.seeded-circle-placement","circle-placements.js","seededCirclePlacement2D"],
  ["layout.seeded-quadrant-partition","quadrant-partition.js","seededQuadrantPartition2D"],
  ["sampling.seeded-triangle-points","triangle-points.js","seededTrianglePoints2D"],
  ["sampling.triangle-coordinate-map","triangle-points.js","mapTriangleCoordinates2D"],
  ["topology.seeded-endpoint-branches-2d","branch-tree.js","seededEndpointBranches2D"],
];
function hash(file){return createHash("sha256").update(readFileSync(file)).digest("hex");}
function display(file){return relative(ROOT,file).split(sep).join("/");}
function files(root){const out=[];for(const entry of readdirSync(root,{withFileTypes:true})){const file=join(root,entry.name);if(entry.isDirectory())out.push(...files(file));else if(entry.isFile())out.push(file);}return out.sort();}
function manifest(root,prefix=root){return files(root).map(file=>({path:relative(prefix,file).split(sep).join("/"),sha256:hash(file)}));}
function write(file,value){mkdirSync(dirname(file),{recursive:true});writeFileSync(file,value);}
function copy(source,target){mkdirSync(dirname(target),{recursive:true});cpSync(source,target);}
function required(file){if(!existsSync(file))throw new Error("Missing CP6 input: "+display(file));return file;}
function run(command,args,options={}){const result=spawnSync(command,args,{cwd:ROOT,encoding:"utf8",timeout:options.timeout??300000,...options});if(result.error)throw result.error;if(result.status!==0)throw new Error(`Command failed: ${command} ${args.join(" ")}\n${result.stdout}${result.stderr}`);return result;}
function json(text,label){text=text.trim();if(!text)throw new Error("Expected JSON from "+label);return JSON.parse(text);}
function sourceHashes(paths){return Object.fromEntries(paths.map(file=>[display(file),hash(file)]));}
function stable(input){for(const [file,value] of Object.entries(input))if(hash(join(ROOT,file))!==value)throw new Error("Source changed during build: "+file);}
function reviewBinding(file){const record=JSON.parse(readFileSync(join(ROOT,file)));if(record.status!=="accepted"||record.owner!=="root"||record.reviewer!=="root")throw new Error("Unaccepted BranchMarks review: "+file);const bindings={};for(const key of ["implementation_sha256","evidence_sha256"]){const values=record[key];if(!values||typeof values!=="object")throw new Error("Missing "+key+" in "+file);bindings[key]={};for(const [path,expected] of Object.entries(values)){const actual=hash(required(join(ROOT,path)));if(actual!==expected)throw new Error("Review binding changed: "+path+" in "+file);bindings[key][path]=actual;}}return {path:file,status:record.status,owner:record.owner,reviewer:record.reviewer,bindings};}
function metadata(){const source=JSON.parse(readFileSync(join(PACKAGE,"package.json")));if(source.name!==NAME||source.version!=="0.1.0")throw new Error("Unexpected source package metadata");return {source,staged:{...source,version:VERSION},transformation:{kind:"staged_package_metadata_version",path:"packages/javascript/package.json",from:"0.1.0",to:VERSION}};}
function server(){return `import {createServer} from 'node:http';import {readFile} from 'node:fs/promises';import {extname,join,normalize,resolve} from 'node:path';const root=resolve('.');const port=Number.parseInt(process.env.PORT??'8765',10);const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8'};function target(value){if(value==='/')return join(root,'index.html');if(value==='/p5.js')return join(root,'node_modules/p5/lib/p5.min.js');if(value.startsWith('/node_modules/@procedurals/javascript/'))return join(root,value.slice(1));if(/^\\/[a-z0-9-]+\\.(html|js)$/i.test(value))return join(root,value.slice(1));return null;}const server=createServer(async(req,res)=>{const file=target(new URL(req.url,'http://localhost').pathname);if(!file||!normalize(file).startsWith(root)){res.writeHead(404);res.end();return;}try{res.writeHead(200,{'content-type':types[extname(file)]||'application/octet-stream'});res.end(await readFile(file));}catch{res.writeHead(404);res.end();}});server.listen(port,'127.0.0.1',()=>{const a=server.address();console.log(JSON.stringify({event:'ready',url:'http://127.0.0.1:'+(typeof a==='object'&&a?a.port:port)}));});\n`;}
function starterPackage(tarball){return JSON.stringify({name:"procedurals-branch-marks-starter",private:true,version:VERSION,type:"module",scripts:{start:"node server.mjs"},dependencies:{[NAME]:`file:vendor/${tarball}`,p5:"2.3.2"}},null,2)+"\n";}
function starterReadme(tarball){return `# BranchMarks browser starter

Run \`npm install\`, then \`npm start\` and open the printed URL. The adjacent \`${tarball}\` is installed locally with p5 2.3.2.

Use **R** for the next seed, **N** for one more generation, **G** to narrow the rule spread, **W** to widen it, **B** for binary slots, and **X** to transfer the root into a forest. These controls rebuild the retained tree geometry. **M** toggles taper and terminal dots, while **C** changes the palette; both restyle the same retained geometry. **0** resets the initial settings and **S** saves the displayed PNG.

Edit \`branch-marks.js\` to change root placement and the fixed rule schedule, or \`sketch.js\` to draw the retained segments, ancestry, generations, nominal lengths, and terminal membership differently. Forest circles reserve space for root poses; they do not keep the branch canopies apart.

This independently composed example is motivated by \`survey/out/2018/Generativos/arbolito3/notes.md\` and \`survey/out/2018/Generativos/arbolito4/notes.md\`. Its schedule, placement, palette, and mark treatment are editable example choices, not library defaults or measured recommended ranges.
`;}
function transform(starter){const index=readFileSync(join(BRANCH,"index.html"),"utf8"),model=readFileSync(join(BRANCH,"branch-marks.js"),"utf8"),sketch=readFileSync(join(BRANCH,"sketch.js"),"utf8");const imports=["../../src/circle-placements.js","../../src/branch-tree.js"],map=`<script type="importmap">{"imports":{"${NAME}":"/node_modules/${NAME}/src/index.js","${NAME}/":"/node_modules/${NAME}/"}}</script>`;for(const path of imports)if(model.split(JSON.stringify(path)).length!==2)throw new Error("Unexpected BranchMarks import: "+path);if(index.split("</head>").length!==2)throw new Error("Unexpected BranchMarks HTML");let generatedModel=model;for(const path of imports)generatedModel=generatedModel.replace(JSON.stringify(path),JSON.stringify(NAME));const generatedIndex=index.replace("</head>",map+"</head>");write(join(starter,"index.html"),generatedIndex);write(join(starter,"branch-marks.js"),generatedModel);write(join(starter,"sketch.js"),sketch);return {rewrites:{"index.html":{inserted:map},"branch-marks.js":{from:imports,to:NAME},"sketch.js":{byte_identical:true}},originals:{index,model,sketch},generated:{index:generatedIndex,model:generatedModel,sketch}};}
function consumerSmoke(starter,sourceModel){const script=join(starter,".consumer-smoke.mjs");write(script,`import {createHash} from 'node:crypto';
import {createBranchComposition} from './branch-marks.js';
import {createBranchComposition as createSourceBranchComposition} from ${JSON.stringify(pathToFileURL(sourceModel).href)};
import * as packageApi from '${NAME}';
const operations=${JSON.stringify(OPERATIONS)};
for(const [,module,name] of operations){if(typeof packageApi[name]!=='function')throw new Error('missing installed export '+name);const url=new URL('./'+module,await import.meta.resolve('${NAME}')).href;const direct=await import(url);if(!url.includes('/node_modules/@procedurals/javascript/src/'+module)||direct[name]!==packageApi[name])throw new Error('installed direct-module identity/origin mismatch for '+name);}
if(typeof packageApi.BranchTreeError!=='function')throw new Error('missing installed BranchTreeError');
const resolved=await import.meta.resolve('${NAME}');if(!resolved.includes('/node_modules/@procedurals/javascript/src/index.js'))throw new Error('package resolved outside installed node_modules: '+resolved);
const branchUrl=new URL('./branch-tree.js',resolved).href,branch=await import(branchUrl);if(branch.seededEndpointBranches2D!==packageApi.seededEndpointBranches2D||branch.BranchTreeError!==packageApi.BranchTreeError)throw new Error('branch module identity mismatch');
const cases=[['baseline',42,false,false,false,false,false,[101]],['extended',42,true,false,false,false,false,[192]],['narrowing',42,false,true,false,false,false,[101]],['wider',42,false,false,false,true,false,[101]],['binary',42,false,false,true,false,false,[38]],['forest',42,false,false,false,false,true,[24,30,88,33,19,38,56]],['forestextended',42,true,false,false,false,true,[50,53,179,57,38,77,106]],['forestseed',43,false,false,false,false,true,[30,88,33,19,38,56,2,66]]];
function bits(digest,value){const bytes=new ArrayBuffer(8),view=new DataView(bytes);view.setFloat64(0,value,false);digest.update(new Uint8Array(bytes));}
function integer(digest,value){const bytes=new ArrayBuffer(8),view=new DataView(bytes);view.setBigInt64(0,BigInt(value),false);digest.update(new Uint8Array(bytes));}
function fingerprint(create,row){const [id,seed,more,narrowing,binary,wider,forest,counts]=row,composition=create(seed,more,narrowing,binary,wider,forest);if(composition.size!==counts.length||composition.totalSegments!==counts.reduce((a,b)=>a+b,0))throw new Error('unexpected composition dimensions '+id);const digest=createHash('sha256'),segment=new Float64Array(4);integer(digest,composition.size);for(let treeIndex=0;treeIndex<composition.size;treeIndex++){const tree=composition.treeAt(treeIndex);if(tree.size!==counts[treeIndex])throw new Error('unexpected tree count '+id);integer(digest,tree.size);for(let index=0;index<tree.size;index++){tree.segmentInto(index,segment,0);for(const value of segment)bits(digest,value);bits(digest,tree.headingAt(index));bits(digest,tree.lengthAt(index));integer(digest,tree.parentAt(index));integer(digest,tree.generationAt(index));integer(digest,tree.childCountAt(index));}}return digest.digest('hex');}
const installed=cases.map(row=>fingerprint(createBranchComposition,row)),source=cases.map(row=>fingerprint(createSourceBranchComposition,row));if(JSON.stringify(installed)!==JSON.stringify(source))throw new Error('installed BranchMarks geometry differs from repository source');if(new Set(installed).size!==cases.length)throw new Error('BranchMarks cases did not differ');console.log(JSON.stringify({operations:operations.map(([id])=>id),branch_export:true,branch_url:branchUrl,resolved,cases:cases.map(row=>row[0]),geometry_fingerprints:installed,source_geometry_fingerprints:source}));`);const result=json(run("node",[".consumer-smoke.mjs"],{cwd:starter,timeout:180000}).stdout,"installed BranchMarks consumer");rmSync(script,{force:true});return result;}
async function main(){let output=join(OUTPUT_ROOT,"javascript-branch1");const args=process.argv.slice(2);for(let i=0;i<args.length;i++){if(args[i]==="--output")output=resolve(args[++i]??"");else throw new Error("Unknown argument: "+args[i]);}output=resolve(output);if(!output.startsWith(OUTPUT_ROOT+sep)||existsSync(output))throw new Error("Output must be a fresh directory under .work/dist/cp6/javascript-branch1");const meta=metadata();const inputs=[join(PACKAGE,"package.json"),...files(join(PACKAGE,"src")),...files(join(PACKAGE,"examples")),...NOTICES,...BOUND.map(file=>join(ROOT,file)),fileURLToPath(import.meta.url)].map(required);const hashes=sourceHashes(inputs);const reviews=["evidence/conformance/branch-export-compatibility-review.json","evidence/conformance/branch-tree-javascript-root-review.json","evidence/conformance/branch-p5js-native-root-review.json"].map(reviewBinding);for(const [,module,name] of OPERATIONS){required(join(PACKAGE,"src",module));if(!readFileSync(join(PACKAGE,"src/index.js"),"utf8").includes(name))throw new Error("Root package export missing: "+name);}if(!readFileSync(join(PACKAGE,"src/index.js"),"utf8").includes("BranchTreeError"))throw new Error("Root package export missing: BranchTreeError");mkdirSync(output,{recursive:true});const build=join(output,"build"),cache=join(output,".npm-cache");mkdirSync(build);mkdirSync(cache);const env={...process.env,npm_config_cache:cache};const staged=join(build,"package");cpSync(join(PACKAGE,"src"),join(staged,"src"),{recursive:true});cpSync(join(PACKAGE,"examples"),join(staged,"examples"),{recursive:true});for(const notice of NOTICES)copy(notice,join(staged,basename(notice)));write(join(staged,"package.json"),JSON.stringify(meta.staged,null,2)+"\n");write(join(staged,"README.md"),`# Procedurals JavaScript ${VERSION}\n\nLocal operations and BranchMarks retained endpoint branches.\n`);const stagedManifest=manifest(staged);const packed=json(run("npm",["pack","--json"],{cwd:staged,env,timeout:180000}).stdout,"npm pack")[0];if(!packed?.filename)throw new Error("npm pack produced no filename");const tarball=join(output,packed.filename);copy(join(staged,packed.filename),tarball);const unpacked=join(build,"unpacked");mkdirSync(unpacked);run("tar",["-xzf",tarball,"-C",unpacked]);for(const path of ["package/src/index.js","package/src/branch-tree.js","package/src/circle-placements.js","package/examples/branch-marks/index.html","package/examples/branch-marks/branch-marks.js","package/LICENSE","package/THIRD_PARTY_NOTICES.md"])if(!existsSync(join(unpacked,path)))throw new Error("Tarball omitted "+path);if(JSON.stringify(stagedManifest)!==JSON.stringify(manifest(join(unpacked,"package"))))throw new Error("npm tarball differs from staged package");const starter=join(build,"procedurals-branch-marks-browser");mkdirSync(starter);const source=transform(starter);copy(tarball,join(starter,"vendor",packed.filename));for(const notice of NOTICES)copy(notice,join(starter,basename(notice)));write(join(starter,"package.json"),starterPackage(packed.filename));write(join(starter,"README.md"),starterReadme(packed.filename));write(join(starter,"server.mjs"),server());for(const path of ["branch-marks.js","sketch.js","server.mjs"])run("node",["--check",join(starter,path)]);run("npm",["install","--ignore-scripts","--no-audit","--no-fund"],{cwd:starter,env,timeout:180000});const lock=JSON.parse(readFileSync(join(starter,"package-lock.json")));if(!lock.packages?.["node_modules/p5"]?.integrity||lock.packages?.["node_modules/@procedurals/javascript"]?.resolved!==`file:vendor/${packed.filename}`)throw new Error("starter dependencies are not pinned/local");const consumer=consumerSmoke(starter,join(BRANCH,"branch-marks.js"));rmSync(join(starter,"node_modules"),{recursive:true,force:true});const starterManifest=manifest(starter,build),zip=join(output,`procedurals-branch-marks-browser-${VERSION}.zip`);run("zip",["-q","-r",zip,basename(starter)],{cwd:build,timeout:180000});const archiveRoot=join(build,"archive-consumer");mkdirSync(archiveRoot);run("unzip",["-q",zip,"-d",archiveRoot],{timeout:180000});const extracted=join(archiveRoot,basename(starter));run("npm",["ci","--ignore-scripts","--no-audit","--no-fund"],{cwd:extracted,env,timeout:180000});const archiveConsumer=consumerSmoke(extracted,join(BRANCH,"branch-marks.js"));if(JSON.stringify(consumer.geometry_fingerprints)!==JSON.stringify(archiveConsumer.geometry_fingerprints))throw new Error("Extracted consumer geometry differs");rmSync(join(extracted,"node_modules"),{recursive:true,force:true});stable(hashes);const report={status:"passed",scope:"CP6 JavaScript 0.6.0 local package and installed BranchMarks starter checks only; no browser launch, render, registry publication, or support acceptance.",package_version:VERSION,input_sha256:hashes,accepted_operations:OPERATIONS.map(([id,module,name])=>({id,module,export:name})),review_bindings:reviews,artifacts:{npm_tarball:{path:display(tarball),sha256:hash(tarball),entries:manifest(join(unpacked,"package"))},browser_starter_zip:{path:display(zip),sha256:hash(zip),entries:starterManifest}},installed_consumer:{branch_marks:consumer,extracted_archive:archiveConsumer,package_lock_sha256:hash(join(starter,"package-lock.json")),starter_rewrites:source.rewrites},included_file_manifest:{staged_package:stagedManifest,starter:starterManifest},environment:{node:process.version,npm:run("npm",["--version"],{env,timeout:30000}).stdout.trim()}};write(join(output,"build-result.json"),JSON.stringify(report,null,2)+"\n");console.log(JSON.stringify({status:report.status,tarball:display(tarball),starter_zip:display(zip)}));}
main().catch(error=>{console.error(error?.stack??error);process.exitCode=1;});
