{
  "id": "grammar.parallel-token-rewrite",
  "version": "0.1.0",
  "status": "reviewed",
  "role": "transform",
  "decision_cluster": "grammar.parallel-token-rewrite",
  "description": "Rewrite explicit token productions in simultaneous generations with bounded token and work counts.",
  "capability_decision": "design/capabilities/p5-tenfold.md",
  "design_review": "design/operations/p5-tenfold-systems-contract-review.md",
  "provenance": [],
  "input_schema": {
    "type": "object",
    "properties": {
      "axiom": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        },
        "minItems": 0,
        "maxItems": 4294967295
      },
      "rules": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "symbol": {
              "type": "string",
              "minLength": 1
            },
            "replacement": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "minItems": 0,
              "maxItems": 4294967295
            }
          },
          "required": [
            "symbol",
            "replacement"
          ],
          "additionalProperties": false
        },
        "minItems": 0,
        "maxItems": 4294967295
      },
      "iterations": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "maxTokens": {
        "type": "integer",
        "minimum": 0,
        "maximum": 4294967295
      },
      "maxWork": {
        "type": "integer",
        "minimum": 0,
        "maximum": 9007199254740991
      }
    },
    "required": [
      "axiom",
      "rules",
      "iterations",
      "maxTokens",
      "maxWork"
    ],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "tokens": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        },
        "minItems": 0,
        "maxItems": 4294967295
      }
    },
    "required": [
      "tokens"
    ],
    "additionalProperties": false
  },
  "fixture_format": "exact-json-output",
  "output_invariants": "Returned tokens preserve generation/production order and do not exceed maxTokens; unmatched tokens pass through once per generation.",
  "behavior": {
    "validation": "Exactly the input and nested-union keys shown by the schema, ordinary dense arrays and passive own data properties. Numeric carriers are finite binary64 and never booleans; integral fields are exact integers in schema domain. Reject accessors, host instances and coercion. Complete static validation of every field/element precedes work preflight, RNG draws or output allocation. Products defining array lengths must be exact and <=4294967295 or INVALID_INPUT, even if all individual dimensions are valid. Canonicalize input zeros to +0.",
    "work": "maxWork is a required nonnegative safe integer. Count arithmetic is exact; an unsafe work sum/product or insufficient allowance is WORK_LIMIT. A charge checks before its action; rejected actions do not execute. The operation-specific schedule is normative. Static input validation has unavoidable input-sized cost. Budgets are abstract work, not memory or latency guarantees; host allocation failure remains a host error.",
    "numerics": "Separate binary64 primitive rounding, no reassociation/FMA. Every computed primitive must be finite or NUMERIC_OVERFLOW, including division by an underflowed zero denominator. No numerical repair, implicit epsilon or state clipping. Exposed zeros are +0. General finite output fields use absolute 1e-9 plus relative 1e-12; integer/topology/order/state/token values compare exactly. The bound fixture file contains only exact analytical outputs; approximate RK4 linear-field and nonzero-angle turtle geometry tests remain separate future property tests.",
    "ownership": "Read only caller inputs; output records and arrays are detached. No input record, array, RNG object, renderer, host state or callback is retained. All generated state remains private until success. No partial result on error; a successful RK4 boundary termination is its explicit retained-prefix result, not an error. Scalars and immutable strings are values.",
    "tokens": "A token is a nonempty string treated as one atomic symbol. Compare exact UTF-16 code-unit sequences with no Unicode normalization or character splitting. Rule/command token keys are unique; unknown tokens follow the operation-specific passthrough or unknown-token policy.",
    "algorithm": "Parallel token grammar rewriting\n\nProposed ID: `grammar.parallel-token-rewrite`; transform.\n\nArtist output: a rewritten token sequence reusable for substitution tile bands or\ngeneration-stratified botanical text. Turtle interpretation is separate. No character\nsplitting, production probabilities, context sensitivity or symbol drawing is hidden.\n\nInput:\n\n```text\n{axiom:[token,...], rules:[{symbol:token,replacement:[token,...]},...],\n iterations:integer in [0,4294967295], maxTokens:integer in [0,4294967295],\n maxWork:integer}\n```\n\nA token is a nonempty string compared by exact UTF-16 code-unit sequence, without Unicode\nnormalization. Rule symbols must be unique. Empty axiom, empty rules and empty replacement\nare legal. Output `{tokens:[token,...]}`. An unmatched token passes through once. Each\ngeneration reads only the previous generation, in order; replacements are not rewritten\nagain until the next generation. Zero iterations returns a detached axiom.\n\nAfter static validation, atomically charge `W0=axiom.length+rules.length+sum(replacement.length)`\nfor setup, then fail OUTPUT_LIMIT if axiom exceeds maxTokens. Before each generation charge1,\nincluding an empty generation. For each old token charge1 for its lookup; determine its\nreplacement/passthrough, check proposed new length<=maxTokens, then atomically charge its\noutput length before appending. OUTPUT_LIMIT precedes the emission charge for that token.\nWork/storage follows total visited/emitted tokens, with at most two generations live.\nString comparison/hashing additionally depends on token text lengths; maxWork is token-work,\nnot a character-processing or byte bound. It does not authorize retaining every generation.\n",
    "native_surface": "p5.js named export parallelTokenRewrite(input) from packages/javascript/src/parallel-token-rewrite.js; plain output matching output_schema. Other targets deferred."
  },
  "errors": {
    "INVALID_INPUT": "Malformed shape, supplied scalar/carrier/domain, impossible array representation, or operation-specific static constraint.",
    "WORK_LIMIT": "The exact required or next charged work exceeds maxWork or safe work representation.",
    "OUTPUT_LIMIT": "Initial or proposed generation length exceeds maxTokens; no partial generation is returned."
  },
  "parameters": {
    "axiom": {
      "unit": "ordered atomic token strings",
      "default": null,
      "encouraged_range": null,
      "evidence": "Independent p5-only design proposal; no corpus-observed default or measured recommended range."
    },
    "rules": {
      "unit": "ordered simultaneous token productions",
      "default": null,
      "encouraged_range": null,
      "evidence": "Independent p5-only design proposal; no corpus-observed default or measured recommended range."
    },
    "iterations": {
      "unit": "rewrite generation count",
      "default": null,
      "encouraged_range": null,
      "evidence": "Independent p5-only design proposal; no corpus-observed default or measured recommended range."
    },
    "maxTokens": {
      "unit": "maximum retained generation token count",
      "default": null,
      "encouraged_range": null,
      "evidence": "Independent p5-only design proposal; no corpus-observed default or measured recommended range."
    },
    "maxWork": {
      "unit": "abstract operation-specific work units",
      "default": null,
      "encouraged_range": null,
      "evidence": "Independent p5-only design proposal; no corpus-observed default or measured recommended range."
    }
  },
  "targets": {
    "processing-java": {
      "required": false,
      "implementation_status": "deferred",
      "native_status": "deferred",
      "reproduction_status": "deferred",
      "delivery_scope": "Deferred by maintainer scope; no support claim."
    },
    "p5js": {
      "required": true,
      "implementation_status": "pending",
      "native_status": "unvalidated",
      "reproduction_status": "unvalidated",
      "delivery_scope": "p5-only independent design studies; no original reconstruction claim."
    },
    "py5": {
      "required": false,
      "implementation_status": "deferred",
      "native_status": "deferred",
      "reproduction_status": "deferred",
      "delivery_scope": "Deferred by maintainer scope; no support claim."
    },
    "processing-android": {
      "required": false,
      "implementation_status": "deferred",
      "native_status": "deferred",
      "reproduction_status": "deferred",
      "delivery_scope": "Deferred by maintainer scope; no support claim."
    }
  },
  "fixtures": "fixtures/operations/parallel-token-rewrite.json",
  "reproduction": {
    "level": "technique",
    "status": "unvalidated",
    "acceptance": "Two original p5 design studies, explicit initial state and bounded replay counts, meaningful structural edits and output reuse, native reset/replay/save and root image review. No original recreation or other-target claim."
  },
  "review": {
    "owner": "root",
    "reviewer": "root after independent architecture and analytical review",
    "status": "Approved for p5 implementation against frozen catalog; support acceptance pending execution."
  }
}
