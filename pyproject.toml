[project]
name = "procedurals-tooling"
version = "0.0.0"
description = "Corpus ingestion and cross-language conformance tooling for procedurals"
requires-python = ">=3.11"
dependencies = [
  "pillow>=12.3,<13",
]

[tool.uv]
package = false
