import { createFieldStudySketch } from "../materials-a-fields-controls.js";
import { drawReducedMosaic } from "../materials-a-studies.js";
createFieldStudySketch({ slug: "reduced-mosaic", title: "Reduced mosaic", draw: drawReducedMosaic });
