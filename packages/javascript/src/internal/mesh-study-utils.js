export class MeshError extends Error{constructor(code){super(code);this.code=code;}}
export const fail=c=>{throw new MeshError(c);};export const fin=v=>typeof v==="number"&&Number.isFinite(v);export const num=v=>{if(!fin(v))fail("INVALID_INPUT");return v===0?0:v;};export const calc=v=>{if(!Number.isFinite(v))fail("NUMERIC_OVERFLOW");return v===0?0:v;};
export function rec(v,ks){if(!v||typeof v!=="object"||Array.isArray(v)||(Object.getPrototypeOf(v)!==Object.prototype&&Object.getPrototypeOf(v)!==null)||Reflect.ownKeys(v).length!==ks.length)fail("INVALID_INPUT");for(const k of ks){const d=Object.getOwnPropertyDescriptor(v,k);if(!d||!("value"in d))fail("INVALID_INPUT");}}
export const get=(v,k)=>{const d=Object.getOwnPropertyDescriptor(v,k);if(!d||!("value"in d))fail("INVALID_INPUT");return d.value;};export function arr(v,n){if(!Array.isArray(v)||Object.getPrototypeOf(v)!==Array.prototype||(n!==undefined&&v.length!==n)||Reflect.ownKeys(v).length!==v.length+1)fail("INVALID_INPUT");for(let i=0;i<v.length;i++)get(v,String(i));}
export const integer=(v,a,b)=>{const n=num(v);if(!Number.isSafeInteger(n)||n<a||n>b)fail("INVALID_INPUT");return n;};export const work=v=>integer(v,0,Number.MAX_SAFE_INTEGER);export const charge=(v,max)=>{if(!Number.isSafeInteger(v)||v>max)fail("WORK_LIMIT");};export function points(v,n,d){arr(v,n);return v.map((_,i)=>{const p=get(v,String(i));arr(p,d);return Array.from({length:d},(_,j)=>num(get(p,String(j))));});}
