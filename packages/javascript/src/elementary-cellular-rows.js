import { array, charge, fail, get, integer, product, record, scalarArray, work } from "./internal/systems-a-utils.js";
const KEYS=["initial","rule","rows","boundary","maxWork"];
export function elementaryCellularRows(input) {
  record(input,KEYS); const initial=scalarArray(get(input,"initial")); if(initial.length<1)fail("INVALID_INPUT"); for(const c of initial)if(c!==0&&c!==1)fail("INVALID_INPUT"); const rule=integer(get(input,"rule"),0,255),rows=integer(get(input,"rows"),1,4294967295),boundary=get(input,"boundary"),max=work(get(input,"maxWork")); if(boundary!=="ZERO"&&boundary!=="WRAP")fail("INVALID_INPUT"); const size=product(initial.length,rows); const cost=initial.length+4*initial.length*(rows-1); charge(cost,max); const cells=new Array(size); for(let x=0;x<initial.length;x++)cells[x]=initial[x]; for(let y=1;y<rows;y++)for(let x=0;x<initial.length;x++){const prev=(y-1)*initial.length,left=boundary==="WRAP"?cells[prev+(x+initial.length-1)%initial.length]:(x?cells[prev+x-1]:0),center=cells[prev+x],right=boundary==="WRAP"?cells[prev+(x+1)%initial.length]:(x+1<initial.length?cells[prev+x+1]:0);cells[y*initial.length+x]=(rule >>> (4*left+2*center+right))&1;} return {columns:initial.length,rows,cells};
}
