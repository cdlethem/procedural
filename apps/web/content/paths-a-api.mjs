const guide = (title, summary, useWhen, inputs, output, code, tryThis, pitfalls) => ({ title, summary, useWhen, inputs, output, code, tryThis, pitfalls });
export const pathsAApiGuides = {
  "geometry.chaikin-polyline-2d": guide("Refine a polyline with corner cuts", "Cuts each edge at fixed quarter positions.", "Use it to soften an explicit route while keeping its endpoints or loop structure.", { points: "Two or more [x,y] points.", closed: "Whether to include the closing edge.", iterations: "Number of refinement passes.", maxWork: "Required bounded-work budget." }, "{points}, a detached refined path in traversal order.", `const result = chaikinPolyline2D({ points: [[0,0],[4,0],[4,4]], closed: false, iterations: 2, maxWork: 21 });\nconsole.log(result);`, "Increase iterations for rounder cuts.", ["Open paths retain both endpoints on every pass."]),
  "geometry.simplify-polyline-2d": guide("Keep decisive polyline turns", "Retains an open path's endpoints and the points beyond its tolerance.", "Use it to turn sampled gestures into editable skeletons.", { points: "One or more [x,y] points.", tolerance: "Nonnegative finite-segment distance threshold.", maxWork: "Required quadratic work budget." }, "{points,sourceIndices}, detached retained vertices and their original indices.", `const result = simplifyPolyline2D({ points: [[0,0],[1,1],[2,0]], tolerance: .5, maxWork: 9 });\nconsole.log(result);`, "Raise tolerance to remove smaller bends.", ["This is explicitly open; choose a loop seam before calling it."]),
  "geometry.offset-polyline-2d": guide("Make signed route margins", "Builds a joined parallel route using left normals.", "Use it for road edges, contour rings, and paired strokes.", { points: "Two or more [x,y] vertices.", closed: "Whether the final edge returns to the first vertex.", distance: "Signed offset distance.", miterLimit: "Maximum miter multiple, at least one.", maxWork: "Required bounded-work budget." }, "{points}, detached offset vertices; bevels can add vertices.", `const result = offsetPolyline2D({ points: [[0,0],[2,0],[2,2]], closed: false, distance: 1, miterLimit: 2, maxWork: 12 });\nconsole.log(result);`, "Use positive and negative distances to draw paired margins.", ["It does not repair self intersections."]),
  "geometry.convex-hull-2d": guide("Find an outer site envelope", "Returns the counter-clockwise hull of scattered sites.", "Use it to frame point clouds or make island-like perimeters.", { points: "Possibly empty [x,y] sites.", maxWork: "Required quadratic-plus-linear work budget." }, "{points,sourceIndices}, detached hull coordinates and earliest source identities.", `const result = convexHull2D({ points: [[0,0],[1,0],[1,1],[0,1],[.5,.5]], maxWork: 30 });\nconsole.log(result);`, "Draw the returned points as a closed p5 shape.", ["Collinear interior sites are omitted."]),
  "geometry.triangulate-simple-polygon-2d": guide("Split a strict polygon into ears", "Produces deterministic triangle indices for a simple nondegenerate boundary.", "Use it to color facets inside a supplied silhouette.", { points: "At least three distinct [x,y] boundary vertices.", maxWork: "Required cubic-plus-quadratic work budget." }, "{points,triangles}; triangles are original point indices with positive working winding.", `const result = triangulateSimplePolygon2D({ points: [[0,0],[4,0],[4,4],[2,2],[0,4]], maxWork: 150 });\nconsole.log(result);`, "Fill each returned index triple with a palette color.", ["Touching, self-intersecting, repeated, and collinear-boundary polygons are rejected."]),
};
