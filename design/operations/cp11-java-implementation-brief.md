# CP11 Java implementation brief

Assignment starts only after root records contract acceptance. Root owns the catalog,
shared fixtures, architectural decisions and final acceptance. Java only; ports remain
deferred. Sol review remains paused for this sprint.

Implement `org.procedurals.paths.OccupiedLatticePaths2D` against the accepted catalog
and shared fixture file. Read the portable-operation-implementation,
deterministic-generative-semantics and generative-performance skills. Reuse established
Java numeric carrier, exception, fixture-runner and Java 8 compilation conventions.
Do not change other public operations or introduce a general graph/session framework.
Return consequential ambiguities to root rather than selecting new semantics.

Own one coherent core slice: Java class, focused native checks and a runner following
existing tools/run_target_springs_java.py conventions. Do not copy the spring-specific
reproduction/attestation machinery. Core conformance is separate from renderer acceptance.
No Processing renders, package release, shared catalog edits or support claims in this slice.

Verify shared exact vectors, then the distinct native risks: caller/result detachment,
all access overloads and field-ordered error precedence, atomic cellInto failure, malformed
numeric carriers, exact wide potential-count rejection, sparse use of maximum dimensions,
and linked-list input traversal. Check the three-neighbour choice, single-choice RNG
consumption, global occupancy and returned-state continuation against the shared vectors.
Do not inflate tests with exhaustive combinations of equivalent accessor paths.

Use packed retained coordinates and sparse primitive occupancy without per-cell inner-loop
allocation. Bound growth by admitted potential cells; never preallocate maxCells or the
whole grid. A small representative-range benchmark should expose allocation and scaling,
including sparse large dimensions; it is not a universal latency guarantee. Preserve failures
under .work and fix causes. Do not repeat successful checks unless changed code warrants it.

Handoff includes changed files, exact commands/results, remaining limits and any contract
questions. Worker success does not constitute acceptance. Root reviews the implementation
and evidence before the separate LatticeMarks starter slice begins.
