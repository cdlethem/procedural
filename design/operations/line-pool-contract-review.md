# Root contract review: seeded line-pool2D

Root accepts the specified computation for Java-first implementation. The reviewed admission,
parameter experiment, exact transition draft, reproducible numeric decision and error review
are the basis; native production implementation, transfer and distribution are not accepted
by this document. Sol remains paused under maintainer instruction.

Independent evidence: root-derived first-choice word traces match the corrected object-list
oracle; eight manually derived zero/short/overflow cases match an independently run snapshot;
three conflicting-error cases confirm precedence. Candidate oracle fixture review is recorded
in evidence/investigations/line-pool-oracle-review.json. Provisional wrong seed-search output
was preserved and excluded. The final catalog uses the corrected outputs only.

The operation retains mutable-cut semantics without a grammar, callback bag or ancestry
fiction. Exact fdlibm5.3 trig results are necessary because future skip decisions read prior
coordinates. Java StrictMath is the first realization. Other targets remain unimplemented,
with exact math support required before any claim. Processing prototype frames establish
control mechanism only: production xoshiro/binary64 output still needs native visual review.

Native implementation must validate passive inputs, nonfinite numeric values, boolean/type
coercion, cap/static precedence, detached access, atomic failed writes, output invariants and
exact fixture/error values. Access obligations are not discharged by pure generation goldens.
Native second seed/stroke transfer and30 independent-pool workload remain required before
capability acceptance. No public defaults or continuous encouraged ranges are asserted.
