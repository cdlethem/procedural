# CP9 Delaunay contract review

Root owns this review under the maintainer's Java-first, paused-Sol sprint policy.
Contract acceptance is not implementation, performance, renderer or release acceptance.
The canonical catalog is the behavioral authority; the design decision memorandum
records how the boundary was chosen.

## Findings resolved

The admitted task is supplied sites to reusable facets and unique edges. Source
scatter/style/third-party behavior remains outside. Root read decisive datata and
lightcity evidence and source calls; the dependency admission preserves their
ordinary candidate accounting. No family-wide merge or Voronoi rejection is implied.

Root reviewed the exact predicate specification, canonical topology and complete
charged schedule. Complete static validation precedes budget checks; the first
charge is atomic N; queue removal follows its charge. The review corrected the
private oracle's precharge coordinate copy and dead-node retention, and clarified
that storage includes the N-entry input mapping even when duplicates collapse.
Native accepted numeric carriers, integral floating fields, accessor domains,
buffer widths and error precedence are specified before Java implementation.

The corrected Fraction oracle supplies golden outputs and work counts. Root hand
checks in cp9-fixture-policy.md support small totals and the full nine-site lattice
schedule. Geometry validation separately uses a lifted four-by-four permutation
determinant and gift-wrap hull, along with topology, incidence, crossing, coverage
and cocircular-diagonal checks. It does not regenerate topology using the production
algorithm. The original prototype's broad queue counts are not reused.

The 36 shared cases include empty/small/all-identical/collinear inputs; duplicate
and signed-zero mappings; reordered inputs; face and edge insertions; exact budget
and every reachable one-short stage; static error precedence; integral-floating
work; minimum-subnormal orientation and incircle; cancellation; near-circle versus
exact-circle topology; and mixed-extreme finite coordinates exercising incircle.
Coordinate outputs compare exact copied binary64 bits, with positive zero.

The fixture's required source bindings contain repository files only. Ignored
private reports provide historical investigation context but cannot be required
to check or regenerate the shared contract from a normal clone. The generator
supports exact regeneration checks as well as explicit writes.

## Acceptance and remaining gates

The machine-readable root review in
`evidence/investigations/cp9-contract-review.json` records the accepted source and
fixture hashes and checks when integration passes. Before that record exists,
this document is review preparation, not a freeze claim.

After freeze, Java implementation follows cp9-java-implementation-brief.md and
the catalog. Native tests must consume unchanged shared vectors and independently
exercise ownership, all access domains, atomic writes and resource behavior.
Tiny, representative hundreds-of-sites and stress measurements remain required.
FacetMarks must separately demonstrate site edits, retained fill/wire/palette
changes, region-centre transfer and triangle grain, with actual Processing output.

No public Java implementation, additional platform, installed starter, visual
reproduction or new package release is accepted by this contract review alone.
