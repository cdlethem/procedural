# Java completion reconciliation — Java0.35

Root review of pushed baseline d7f95a06f55fd0937ebb2c12bb2192c7644922c4. The five
requirements in docs/java-completion-plan.md remain unchanged. This is a current-state
reconciliation, not a declaration of completion or a new operation admission.

| Requirement | Current evidence | Status and next action |
| --- | --- | --- |
| Install and edit from an extracted distribution | evidence/distribution/contact-java-review.json and consumer:37PDEs compile, installed-JAR ContactMarks5state edit/reset/save passes,6images match reviewed source run | Demonstrated at the recorded runtime. Getting-started links direct Java API and workflow chooser. Preserve extracted proof at integration checkpoints; no new render campaign is needed for unchanged examples. |
| Major idioms and explicit boundaries | Workflow chooser; java-rare-family-review.md; java-family-boundaries-cp27.md; current31operation contracts | Existing reviewed families have entry points. Nearest contact closes the intersection-query dependency, but source-specific two-pass plasma007 composition remains unproven as a whole. Grammar rewriting, nearest-site Voronoi cells, glyph outlines/shaping and arbitrary solid modeling retain explicit unsupported dispositions. Reconcile capability-map wording rather than infer support from a new class count. |
| Reviewed semantics, provenance and native workflows | Root invoked tools.operation_attestations.load_attestations on all current catalog files:31core conformant,31native validated-scoped, no binding errors | Scoped operation evidence present. Public method documentation remains incomplete; inspect missing semantic explanations rather than treating warning count as an acceptance score. |
| Composability and assessed originals | MaskedPartitionMarks, ContactMarks, image placement/sampling/filtering, clipping and retained state examples; recreation-cohort-01.md and four root recreation reviews | Composition demonstrated, including interchangeable local/global/cropped content and explicit obstacle inputs. Three selected cohort originals plus separate Curvespace have historical reviewed structural recreation evidence. No projection to826reports/901targets. Five operations have no technique attestation; retain that fact. |
| Coherent docs/tooling/licensing/performance | Java0.35 source/consumer inventories, exact-input native records, java-performance.md and existing render/sweep tools | Packaging/licensing checks present.707Javadoc warnings include newly visible package-private helper documentation. Complete essential public references and current capability/install guidance before final acceptance; internal warnings and missing tags are not equivalent to an unusable public method. |

## Scope of technique evidence

Current technique statuses:26validated-scoped,5unvalidated: nearest contact, separable blur,
annular solid, sequential disc projection and segment clipping. These five have reviewed
native workflows; that does not imply a technique-level or complete-source reproduction
attestation. Do not silently promote them. Any additional recreation work must name the
original, fidelity target and remaining defining algorithms before it becomes an assignment.

Reviewed original evidence: evidence/reproductions/r1-p3d/root-review.json,
r2-p3d/root-review.json, r3-p2d/root-review.json and curvespace/root-review.json.
Curvespace remains a repository recreation rather than an additional source-bundle workflow.
This reconciliation identifies those historical records; it is not a fresh renderer/runtime
recertification of all four originals.

## Next bounded batch

1. Terra read-only audit of the current Javadoc report and public classes: at most5essential
   missing API explanations, excluding already-corrected direct-use guide topics.
2. Root reviews findings against exact implementations/contracts and chooses one broad
   documentation batch. No new behavior, defaults or unmeasured artistic ranges.
3. Preserve runtime semantics if comments change core sources; use focused equivalence and
   existing review infrastructure. Avoid one release/support migration per edited method.
4. Reconcile the completion/capability/port-resume documents at the next useful checkpoint.
   Ports remain paused. Root retains final integration and any future port acceptance.

The goal remains active. Another operation is not the default next action merely because a
catalog slot or survey candidate is available.

## Bounded documentation audit outcome

Terra identified missing whole source comments on CyclicPalette construction/export/error
members and CirclePlacements2D result accessors. Root inspected both implementations and
closed the reader-facing semantic gaps in docs/java-result-values.md, linked from direct
Java and PlacementMarks guides. No Java source changed; source Javadoc omissions remain
explicit, rather than claiming a clean reference build. No runtime revalidation is needed
for this prose-only batch. Port handoff now pins the pushed Java0.35 commit and distinguishes
new contact semantics, shared arithmetic, masked adapters and workflows from prior cores.

## Release gate reconciliation after installed start-page review

Root reloaded current operation attestations after restoring the accepted PlacementMarks
bytes: all 31 operation bindings validate without errors. The installed start-page review
in evidence/documentation/java-bundle-start-review.json records 37 extracted example
compilations, identical core/adapter class bytes, and 17 resolving README links. The direct
value guide closes the identified reader-facing palette/placement explanations; source
Javadoc omissions remain disclosed and are not a warning-count acceptance target.

Terra performed a bounded read-only challenge against the five unchanged completion
requirements and found no concrete unmet requirement in that scope. Root independently
confirmed the explicit family dispositions: grammar rewriting, font outlines/shaping,
nearest-site polygons and arbitrary solids are unsupported extensions, not admitted
implementations waiting in a queue. Annular topology, polygon clipping and first-contact
queries close the concrete geometry leads raised by those earlier reviews.

Five unvalidated technique attestations remain honest scope limits. They must not be
promoted, but the completion plan requires representative native workflows, not a separate
whole-technique attestation for every operation. Likewise, the completed Java ray-web
transfer is evidenced in evidence/workflows/contact-marks/root-review.json; earlier native
planning prose is not evidence that the transfer is still missing. Full plasma007 recreation
remains unclaimed.

There are zero currently admitted but unimplemented core operations or starter workflows.
This is a queue statement, not a new completeness definition. The next action is root's
final requirement-by-requirement acceptance audit using the current native, reproduction,
performance, tooling and distribution records. The worker's conclusion and this reconciliation
do not themselves grant completion. Do not invent another algorithm or repeat a documentation
cleanup solely because a historical paragraph says that review remains open.

The recipe schema/executor remains a separately labeled draft. Web/MCP architecture can
use the established Java surface, but recipe execution, browser preview and four-target
export need their own acceptance; they are not retroactive Java release requirements.
