# Recreation cohort 01: composition of accepted Java operations

Root selects three original sketches for a bounded report/source-to-package walkthrough:

- 2019/generativos/ciscis002: cell subdivision, triangulation and extruded city facades.
- 2019/generativos/momito: biased subdivision, triangulated wedge relief and spike bars.
- 2019/generativos/parapara: depth-sized circle filtering, triangulated overlay and banded landscape.

These were chosen because their reports describe compositions touching several existing
operations, not because they are known to be fully supported. This is a deliberately selected
cohort of three, not a representative sample or a corpus percentage. No recreation is yet
credited from this assessment. Root has read momito and relevant report sections for the
others; exact source and contract mapping must precede complete-support claims.

For each, enumerate defining computations, map accepted Java operations and retained outputs,
and identify missing semantics. Compare attempts/order/selection policies, palette easing,
point metadata, geometry and renderer requirements. Do not equate triangulation with a full
city or substitute uniform subdivision for biased source selection without recording it.

Classify remaining work explicitly: ordinary drawing/artistic glue, reusable algorithm absent
from the package, or unresolved evidence. A short loop may still implement a missing algorithm;
length alone does not decide. Conversely, building a top triangle and three wall quads may be
ordinary mesh drawing rather than a reason for a new universal extrusion API. Root decides
that boundary after the concrete walkthrough.

Output one concise evidence map with current support status and exact gaps per original,
then root selects one demonstrable recreation or one missing capability with a justified
coverage gain. No new library signatures, bulk ledger adjudication, renderer runs, porting,
or new coverage framework in this assessment. Sol remains paused.

## Demonstrated result

Momito now has an accepted structural/technique recreation with zero new core operations:
see evidence/reproductions/r1-p3d/root-review.json. That is1 demonstrated original in this
selected three-sketch cohort, not a corpus percentage. The other two remain unresolved.

Ciscis002 is now also demonstrated at structural fidelity through CityMarks; see
`evidence/reproductions/r2-p3d/root-review.json`. Current selected cohort:2 demonstrated
originals of3, with parapara unresolved. Neither recreation required a new core operation.

## Completed cohort result

All three selected originals are now demonstrated at declared structural/technique fidelity:
Momito (R1), ciscis002 (R2), parapara (R3). Native/root reviews reside under
`evidence/reproductions/r1-p3d`, `r2-p3d`, and `r3-p2d`. Local Java0.14 packages all three.
No new core operations were introduced. This is a deliberately selected three-sketch test
of composition, not an estimate for901 originals. Earlier paragraphs record assessment
stages and are superseded by this result. Next breadth decision must consider unmet idioms.
