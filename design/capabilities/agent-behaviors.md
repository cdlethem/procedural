# Draw lingering relationships, sensing trails and flocks

Root admission: **reviewed for p5 implementation, not yet target-accepted.** Evidence context is the September 2026
external-art collection, plan B, and technique families 4–7. The plan names Reas *Process 18*,
*Tissue*, and *Phototaxis*, and Hodgin *Fish Tornado*, *Traffic*, and *Collider* as motivating
visible tasks. No cited artwork is claimed recreated, and the authored laws below are not
attributed to those artists.

| Candidate | Artist task | Computation removed | Existing alternative | Reusable result | Meaningful edit | Transfer and evidence limit |
| --- | --- | --- | --- | --- | --- | --- |
| `spatial.contact-history-2d` | Draw links that fade only after an interaction ends. | Stable caller-ID pair reconciliation, tick accounting and expiry. | Radius pairs alone give only current topology; example-local maps hide identity through reorder/delete. | Sorted detached contact ticks keyed by two explicit IDs. | Radius and linger steps change separate visible dimensions. | Fixed chain/ring transfer; proves graph substitution, not Process 18 contact-opacity behavior. |
| `motion.sensor-motor-step-2d` | Make a trail respond to two lateral senses of a scalar image/field. | Probe placement, grid boundary/bilinear sampling, turn and synchronous advance. | A field tracer follows a supplied vector; it does not implement a paired sensor/motor schedule. | Next agents plus exact sampled probes/values. | Sensor angle/distance, turn gain, grid boundary and scalar contrast stay independent. | Luminance-grid transfer; not Tissue/Phototaxis execution. |
| `motion.flock-steer-2d` | Let marks gather, align, and separate while preserving a chosen graph. | Degree-normalized neighborhood position/velocity/separation arithmetic. | Pair force has one positional reciprocal law and cannot align velocity. | Detached steering vectors, composable with any explicit integrator. | Cohesion, alignment, separation and cap remain separately editable. | Radius graph to fixed glyph/ring graph; not traffic rules/fish mesh behavior. |

All three retain artist-owned initial state, field values, graph source, marks, palette, trail
retention, integration/boundary policy where not specified, and p5 drawing. They neither add
multiple species, contact collision, road rules, target selection, predation, random wandering,
nor persistent host objects.

Family 7 remains active in the overall plan but is not bundled into B. Its eventual decision
must select one distinct task—constant acceleration, disc collision, or conserved transfer—and
then specify mass units, integration, contact order, conservation, boundary behavior and native
evidence. Unit-mass pair attraction is not gravity by implication.

Root freezes three separate boundaries after Sol's independent draft challenge. Heading
turns use +x right/+y down; field origin is first-cell center, signed gain reverses response,
and contact counters leave opacity policy to the artist. Reused IDs mean the same identity;
callers must issue a new ID for new entities. Flock returns steering; ordinary integration
remains editable. Modulo wrapping has an explicit rounded-seam guard. Zero coefficients do
not hide overflow. Source motivation uses the September manifest SHA256
c25caa8edf07e11bf1f97cc5a1d70dd65a915620992bc0b3d3746c80ae59d209;
Reas records casey-reas-recjrha9odoj8kk9v and casey-reas-recrvd9by1oqimfxm motivate
history/sensing without specifying these equations. Tests must distinguish ID reorder,
deleted/expired history, shortened linger, field boundaries, paired sensing and alignment.
Native studies need logical-tick replay, meaningful structure/appearance edits, reset/save
and fixed-graph or image-field substitution. Core checks alone do not establish native support.
