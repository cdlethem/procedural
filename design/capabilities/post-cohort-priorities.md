# Root priorities after recreation cohort01

Java0.14 delivers14 operations and14 editable workflows; three selected originals now have
native reviewed structural recreations with zero additional core APIs. This establishes
useful composition, not exhaustive corpus coverage. Stop extending this selected cohort.

## Remaining I2 capability assessment

| artist task / family | delivered evidence | remaining distinction |
|---|---|---|
| Independent field marks and integrated paths | CP1/CP2 | Band-constrained proposals remain CP12 investigation; not all flow behavior is covered |
| Spacing, subdivision, grain and Delaunay | CP3–5,CP9,R1–3 | No blanket Voronoi-cell or arbitrary polygon capability claim |
| Branches | CP6 endpoint branching | Mutable interior-cut line pools remain missing; endpoint trees cannot replace them |
| Profiles and 3D compositions | CP7,R1/R2 | No universal solid extrusion or mesh-processing claim |
| Typography | CP8 glyph placement | Font outlines and arbitrary shaping are not inferred from placement support |
| Physics | CP10 target springs | Scoped target motion, not a general constraint/particle solver |
| Occupied orthogonal paths | CP11 | Not free walkers or contour tracing |

The API-design table's historic 'currently missing' cells predate these deliveries. Consult
catalog attestations and native reviews for implemented scopes; do not interpret that table
as proof the entire old backlog is still unimplemented or entirely complete.

## Next bounded decision: mutable line-pool branching

Artist task: revise existing strokes by cutting them internally and growing branches at the
cuts, retaining the resulting segment pool for different mark styles. Existing endpoint
branching does not mutate parent geometry, select from a worklist or revisit divided segments.
A new shared-pool computation would remove a real algorithmic burden, unlike the last three
examples' drawing glue.

Root reread survey/out/2019/generativos/brotes/notes.md, the existing exact source audit in
branching-evidence-audit.md, and active pinned source lines63–168. Important distinctions:
30 INDEPENDENT pools of90000 attempts, not one shared30-seed pool; index=floor(U*N*V),
V in [.8,1), not tail-only selection; short lines consume attempts; first cuts add0,1,or2
children; repeated else-if branch unreachable; divided parents are cut again and append
continuation plus angled branch. Nested cut random(low,high) sometimes has reversed bounds.
Do not carry the note's plain .6–.8 or1–3-child shorthand into a contract.

There are no completed parameter trials for this report. Source literals are not encouraged
ranges. Next packet is an architecture/parameter investigation only: root specifies which
state transitions a useful operation must preserve; a bounded worker may retrieve a second
computational neighbour and exact source-host reversed-bound semantics. Before exposing
or defaulting controls, apply parameter-evidence workflow. No new renderer framework, public
contract or implementation until that decision is written.

Stopping condition: decide one of (a) a compact line-pool operation worth prototyping, with
explicit count/termination/selection and retained outputs, or (b) documented deferral with
actual interface/evidence costs. Do not start an open-ended family audit. Root owns the
boundary and reads decisive evidence; cheaper models handle exact retrieval only.

## Sequence after that decision

Reassess CP12 against this bounded option; its earlier deferral was prioritization, not
rejection. Schedule only justified Java capability work. Then enter I2's deferred port batch
against frozen contracts, preserving scope and native support claims, before X1 recipes and
exporters and X3 MCP/web. X2 guides/helper tools can improve alongside those steps. No claim
that all milestones are complete or that these two investigations exhaust the corpus.

## Line-pool decision delivered

Java0.15 now delivers15 operations and15 starters, including LinePool2D/CutBranchMarks.
See evidence/distribution/line-pool-review.json. The interior-cut algorithmic gap is closed
for the declared single-pool mechanism; no whole-brotes recreation count is added.
The historical investigation instructions above are complete. Next root reconsiders CP12
from existing evidence only, comparing useful artist edits, algorithmic burden and API cost.
Stop at an explicit admit/defer decision, then schedule I2 ports if further Java breadth is
not justified. No fresh whole-corpus audit or automatic extra starter cohort.
