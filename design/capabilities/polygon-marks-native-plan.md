# PolygonMarks native workflow plan

Root-owned CP20 delivery boundary. Core accepted at28532369; this workflow is not yet accepted.
Artist edits: A changes outline thickness; M replaces rounded outlines with diamonds; C
changes color while retaining the same placement object; R regenerates proposal descriptors;
0 restores seed42/thickness0.8/rounded/baseline color; S saves the cached completed frame.
600 seed-generated center/length/angle descriptors are retained across A/M/C. Geometry
construction is ordinary example code; ConvexPolygonPlacements2D owns all validation and
ordered overlap rejection. Draw returned vertices, not a different display approximation.

Root owns PDE/design/native review. A bounded worker may own tests/native/PolygonMarksProbe.java
and register the profile in existing tools/run_warp_marks_java.py only. Reuse shared machine
lease, source-built candidate JAR, actual key events, framebuffer/cache checks and save gating.
Six states: baseline, thin, recolored, diamonds, regenerated, reset; keys acmr0s. Check actual
JAVA2D512 density1; core class loaded from candidate JAR; shape/ratio/seed per state;
600 attempt count, source-index geometry correspondence; C retains placement/pose identity;
A/M retain poses and replace placements; R changes poses; reset repeats baseline geometry
and pixels; cached save exactly equals final displayed state. No fake renderer or source
recreation claim. Root visually inspects distinct views and refreshes centralized gallery.

Stop at reviewed native workflow before claiming source-bundle acceptance. Keep existing
Java0.22 usable; port integration/Sol paused. Current proposal geometry/600 count are authored
workload settings, not measured recommended ranges for the library.
