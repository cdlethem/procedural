# CP7 capability dependency admitted

Root admitted `mesh.radial-profile-surface-3d` after reviewing the exact source evidence,
the accepted seven-image comparison, and the mechanical ledger proposal. The canonical
ledger prerequisite check passes for contract preparation. This is an independently
specified operation dependency, not a whole-helper merge or implementation approval.

The admission consumes a supplied radial profile and angular subdivision and produces
retained indexed geometry, outward winding, local flat face normals and face provenance.
It removes seam/cap/pole construction and lighting geometry arithmetic from artist drawing.
cilindros, fieeee and Circo remain in their existing candidate clusters. Their palette,
randomness, supplied-profile construction, camera, overlays and renderer work are explicitly
accounted for as remainders. None is falsely relabelled as the new operation.

Exactly one of1,934candidate records changed: `2015/Generativos/FFt/prueba4#0` moved from
provisional merge to reviewed defer. The source's full-length longitudinal strips overlap;
the old contiguous-ring premise was wrong. It stays in the `mesh.revolve` investigation
family, with both parent-note provenance and the corrective source review. No family was
rejected. The resulting counts are26keep,100merge,18reject and1,790review_required.

Root corrected the proposal's doubled `survey/survey` note paths, verified note and
upstream hashes, preserved mandatory parent-note review provenance, and completed the
cilindros randomness/colour remainder. Full before/after record, ledger hashes and review
actions are in [the admission record](../../evidence/investigations/cp7-ledger-admission-review.json).

The source selection and computational responsibility are settled. Exact scalar arithmetic,
normal normalization, finite-but-collapsed geometry, errors, ownership, resource checks and
fixtures are contract work. No public signature or Java implementation is frozen yet.
