# CP11 Java delivery requirements

Package work begins after root accepts LatticeMarks native behavior and visuals. The
accepted core alone is insufficient. Target a local Java0.11.0 archive with14 operations
and11 starters; do not publish a registry release or claim deferred ports.

Root inspected the accepted Java0.10 archive for this handoff:
`.work/dist/cp10/java/procedurals-processing-0.10.0.zip`, SHA256
`f045387a0bc1912795993b08ee7401023731da3bc06d2cc290ba772d9ed08dab`.
Its root acceptance is `evidence/distribution/cp10-review.json`. It contains60 file members,
68 core JAR file entries, and these ten starters: FieldMarks, PathMarks, PlacementMarks,
RegionMarks, GrainMarks, BranchMarks, ProfileMarks, GlyphMarks, FacetMarks, SpringMarks.

Use the existing incremental archive-builder pattern in tools/build_spring_marks_java.py.
Carry all prior starter files and core class entries forward byte-for-byte. Add only the
accepted OccupiedLatticePaths2D class family, staged LatticeMarks helper/PDE, its guide and
installation instructions. Update version metadata and generated reference through their
existing tools. Preserve notices and reject duplicate or unsafe archive members. Compare
archive contents directly; counts alone do not prove preservation.

Bind actual final sources, staged JAR/helper/PDE, native result and root review before build.
Read the actual CP11 evidence shapes rather than assuming all CP10 review keys are identical.
Do not weaken acceptance by checking only a status string. Documentation must describe the
validated controls and explain empty starts/blocked paths. Remove the guide's implementation-
pending label only after acceptance. Root reviews the archive diff and install instructions;
no unchanged native renders need rerunning to validate a packaging-only change.

This brief specifies the next delivery, not evidence that it exists. The complete roadmap,
including remaining Java breadth, deferred ports and other milestones, remains active.
