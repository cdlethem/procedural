# I2 port batch 03: retained triangle grain

Batch02 is complete through local distributions. Next port the two frozen CP5 operations:
`sampling.seeded-triangle-points-2d` and `sampling.triangle-coordinate-map-2d`, then GrainMarks.
Java remains the accepted semantic reference, alongside the language-neutral contracts and
fixtures. This batch adds portability, not a new algorithm, parameter range or Java API.

An artist can scatter repeatable uniform points inside an ordered triangle, or supply unit
coordinate pairs to deliberately concentrate points using the same mapping and drawing code.
Keep these distinct entry points and their shared result. Explicit mapping owns no RNG and
makes no distribution claim. Preserve the source-motivated coordinate substitutions in the
editable starter; do not add source-named presets, callbacks or implicit noise streams.

Implementation must read both full catalog contracts and every fixture section before code.
Review correctly rounded square root, weighted-sum evaluation order, permitted collapsed
triangles, overflow, zero-count validation, count-prefix behavior and seeded/explicit mapping
equivalence. Preserve passive carriers, detached output and atomic accessor error ordering.
Do not infer target conformance from Java or reuse fixture labels without executing outputs.

Use one bounded core assignment for both operations on one target, starting with JavaScript.
Root reviews the full implementation, auxiliary fixture coverage and meaningful host-carrier
checks before integrating public exports. Then port the accepted GrainMarks workflow with
retained style edits and coordinate-source substitution using the existing native harness.
Write its bounded native acceptance plan before execution and use the shared machine lock.
Follow with Python and Android, then local package consumers. Existing core/native/package
claims remain separate; no new reproduction or cross-target pixel-identity claim is implied.

Luna/Terra may implement frozen semantics and debug their focused checks. Root owns contract
ambiguities, integration, representative image review and every support/acceptance record.
Reuse established builders, runtime environments and fixtures. Stop adding checks when the
contract and actual consumer behavior are covered; no new renderer framework or broad
performance matrix. Later recipe/exporter/MCP/web and recreation milestones remain open.
