# I2 port batch 05: retained radial-profile surfaces

Begin from pushed restoration baseline 4905c054bf6540bdade201f3e64e78423b24d53e.
Ten operations have completed four-target local delivery. This batch ports the accepted
mesh.radial-profile-surface-3d contract and ProfileMarks workflow; no new public operation,
profile preset, parameter range or renderer framework is admitted by this packet.

The artist supplies axial positions and radii and receives a retained indexed surface.
The operation removes seam, pole, cap, triangle winding and flat-normal construction.
Band/cell metadata lets appearance change independently of geometry. Camera, lighting,
placement and drawing remain explicit workflow concerns. Java ProfileMarks is the reference
for profile, subdivision, closure and retained-style editing plus transfer composition.

Read the whole catalog contract, CP7 selection and contract review, Java RadialProfile3D,
ProfileComposition and every fixture section. Preserve static validation before count
capacity, exact count arithmetic before allocation, canonical ring/pole/cap order and
outward winding. Normal generation must preserve the specified separately rounded edge,
scale, cross and normalization operations and first dynamic error face/stage. No epsilon,
smooth normals, dropped faces or global tolerance substitute is permitted. The fixture's
component allowances are fixed; zero allowance and same-runtime replay require exact bits.

Port the complete retained interface and detached export, not only the starter's reads.
Preserve passive carriers, destination typing, writable-slot validation, index/error
precedence and atomic three-slot writes. Dynamic-language indices accept integral numbers
but reject booleans; host allocation failures must not become contract validation errors.
Reuse established carrier/access and fixture infrastructure. Avoid per-face temporary
objects in generation and drawing loops.

Bounded ownership: Luna inventories existing harnesses without edits; Terra implements
one frozen-contract target slice at a time, beginning with JavaScript core. Root reviews
numeric/error/ownership boundaries and coverage before the native workflow. Python follows,
then Android execution of the unchanged Java core and a scoped native 3D workflow. A 2D
restoration acceptance does not establish a P3D lifecycle boundary. Sol remains paused.
Workers do not edit shared catalog acceptance, root reviews or shared fixtures.

For each target preregister a small meaningful native scenario set before rendering:
profile/subdivision/closure changes, retained appearance changes, transfer, reset and save.
Reuse runtime and staging infrastructure. All native execution uses the shared machine
lock. Keep core, native workflow, reproduction and distribution claims separate. Close
with installed package consumers and root review. Escalate genuine contract ambiguity;
do not expand tolerance, weaken a check or introduce infrastructure to conceal a failure.

Stopping conditions for each assignment are a reviewed core and focused fixture/access
results, then separately reviewed native evidence and local packages. Four operation ports
remain after this batch: Delaunay, springs, occupied lattice paths and mutable line pools.
The broader artist/reproduction/recipe/tooling milestones remain open.
