# Creative-quality source checkpoint (draft)

Revision `3fcb389d` carries a bounded revision of 26 existing p5 gallery studies. It is a
source checkpoint, not a new operation or target-acceptance claim. The reviewed current
site and the separate landing-page redesign were preserved during integration.

The batch replaces fixed compositions or near-cosmetic controls in Ornament Field,
Shape Matrix, Orbital Brush, Embossed Field, Signed Edge Print, five path studies,
six mesh studies and four cellular studies. It also gives Warp Marks, Blur Marks and
four Materials A color fields editable source coverage, so those raster layers can
leave clear space for lower layers. Old saved parameter shapes have explicit legacy
branches; the modern controls admit exact numeric entries beyond the convenient slider
span where justified. The Studio document still owns the background.

The isolated 95-study checkout passed `npm run check:generated` (95 workflows, 69 API
entries), `npm run typecheck`, 57 focused web tests and `npm run build`. Real Studio
browser comparisons and native p5 browser checks covered changed geometry, source
choices, alpha, export, reset/reload and layered compositions. The root reviewed the
representative images. The machine-local navigation index is
`.work/visual-review/index.html`; its images are deliberately outside Git.

The repository-wide `uv run python tools/check_catalog.py` remains red in a fresh
checkout. Historical reviews bind ignored browser images that are absent there, and
the present source edits change files bound by older acceptance records. The exact
successor chain and evidence archival need separate review. No previous acceptance
record was rewritten, no incomplete support was marked accepted, and this checkpoint
does not certify the full surveyed corpus. The separate 104-study dynamics candidate
also remains outside this 95-study checkpoint.

Next integration work is to reconcile the historical bindings, review the remaining
gallery families against the same creative standard, and promote only a tested release
that preserves the established 8443 site baseline.
