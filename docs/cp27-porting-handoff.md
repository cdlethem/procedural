# CP27 Java0.30 porting handoff (historical milestone)

For a new assignment, use [the current port handoff](porting-resume.md). Counts and
artifact identifiers below describe this historical milestone, not current main.

This checkpoint added
geometry.sequential-disc-projection-2d and ProjectionMarks to the CP26 baseline:28 Java
operations and32 workflows. The earlier handoff is cp26-porting-handoff.md.

Port the immutable catalog semantics and15 shared fixtures. Java native cases additionally
cover strict numeric carriers, LinkedList conversion, detached ownership and atomic
pointInto errors. Preserve ordered binary64 arithmetic, scaled norm, explicit center
direction, overflow distinctions, work preflight and error precedence. This is deformation,
not a collision solver or Voronoi implementation. No complete original recreation claimed.

ProjectionMarks demonstrates contour and open-line transfer, strength/order edits and
recoloring while retaining six results. The Java extracted workflow passes eight states.
No non-Java support is attested by this milestone. Root
reviews semantic decisions, native outputs and patches before merging or attesting support.
Workers must not write root acceptance records. Coordinate renders through the one
machine-wide tools/with_native_render_lock.py lease across all checkouts.

Later Java completion is recorded in [the completion review](../evidence/distribution/java-completion-review.json). Preserve licenses/provenance and exclude images/builds/copied assets.
